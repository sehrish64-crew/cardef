# Local Development Environment Variables Template
# Copy this to .env.local and update values for local development.

# Base URL for local dev
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=carvertical
DB_PORT=3306

# Admin login
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me

# SMTP Email Configuration
# For Gmail: Use port 465 with SMTP_SECURE=true
# If 2FA is enabled on Gmail account, create an App Password at: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=sehrishnadeem39@gmail.com
SMTP_PASS=fmgy swpy uhxk pyge
EMAIL_FROM="Carreaders <no-reply@localhost>"

# Optional email provider credentials
RESEND_API_KEY=

# Optional local API keys
AUTO_DEV_API_KEY=

# Debug / feature flags
ENABLE_DB_CHECK=false
